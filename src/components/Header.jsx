import React from "react";

function Header() {
  // the function name of the custom component should be in
  // PascalCase
  return <header>
<h1> Keeper / Header</h1>
   </header>
}

export default Header;
